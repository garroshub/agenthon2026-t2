# Agenthon 2026 Track 2 submission V2-safe

Pure numerical probabilistic forecasting runtime for Agenthon 2026 Track 2. V2-safe preserves the V1 primary forecast on clean inputs and adds layered factor-specific, universal, and final contract-preserving fallbacks so numerical-path exceptions do not become container failures. V2-safe preserves the V1 primary forecast on clean inputs and adds layered factor-specific, universal, and final contract-preserving fallbacks so numerical-path exceptions do not become container failures.

The release image exposes the official `forecast` command and is published as `ghcr.io/garroshub/agenthon2026-t2:v5-m0-raw-probe`.
